import { OverdueServersPipe } from './overdue-servers.pipe';

describe('OverdueServersPipe', () => {
  it('create an instance', () => {
    const pipe = new OverdueServersPipe();
    expect(pipe).toBeTruthy();
  });
});
